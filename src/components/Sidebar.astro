---
import { brandSettings } from '../scripts/brandSettings.js';

const normalizePublicPath = (path) => {
    if (!path) return '';
    return path.startsWith('public/') ? `/${path.slice(7)}` : path;
};

const logoSrcLight = normalizePublicPath(brandSettings.logo.src);
const logoSrcDark = normalizePublicPath(brandSettings.logo.srcDark || brandSettings.logo.src);
---
<aside class="sidebar">
    <header class="sidebar-header">
        <div class="brand">
            {logoSrcLight ? (
                <img
                    id="brand-logo"
                    src={logoSrcLight}
                    data-logo-light={logoSrcLight}
                    data-logo-dark={logoSrcDark}
                    alt={brandSettings.logo.alt}
                    style="max-width: 150px; height: auto; margin-bottom: 1rem;"
                />
            ) : (
                <div class="placeholder-box logo-placeholder">
                    [Brand Logo Image Placeholder]
                </div>
            )}
            <h1 class="brand-title">Brand Guide</h1>
        </div>

    <button id="mobile-toc-toggle" class="mobile-toc-toggle" aria-label="Toggle Table of Contents" aria-expanded="false">
        &#8942;
    </button>
    </header>

    <nav id="site-navigation" class="toc-nav" aria-label="Table of Contents">
        <strong>Table of Contents</strong>
        <ul id="toc-list">
            <!-- Links will be dynamically populated here by JavaScript -->
        </ul>

        <button id="theme-toggle" class="theme-toggle" aria-label="Toggle Dark Mode">
            <span class="theme-icon-light">☀ Light</span><span class="theme-icon-dark">☾ Dark</span>
        </button>
    </nav>
</aside>