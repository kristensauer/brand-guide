---
import InstructionNote from '../InstructionNote.astro';
import ColorSwatch from '../ColorSwatch.astro';
import { brandSettings } from '../../scripts/brandSettings.js';
--->
<section id="color">
                <h2>Colors</h2>
                


                <h3>Brand Palette</h3>
                <ColorSwatch />

                <h3>Color Usage Policy</h3>
                <p><strong>Use of Color:</strong> Color is an important aspect of Splitli’s identity, but color alone is never used to communicate meaning, state, or important information. Some users have color deficiencies, are low vision, or use assistive technologies that do not interpret color differences. Because of this, every color-based element has an additional indicator such as text labels or icons. </p>

                <h3>Text Contrast Playground</h3>
                <p>Splitli follows the WCAG 2.1 AA contrast standards to ensure text remains readable for users with low vision or visual impairments. For normal text, the ratio requirement is 4.5 : 1, and large text (18+ points or 14+ points bold) is 3 : 1. Higher contrast improves the readability and experience for everyone.</p>
                <p>Below is the Text Contrast Playground, which allows designers to test color combinations before using them in designs. To use the tool, first select a background color from the dropdown menu. Next, choose a text color from the menu as well. After selecting these items, a visual representation will appear below with a contrast ratio, as well as a grid with WCAG 2.1 AA and AAA pass/fail indicators.</p>
                <div id="contrast-calculator" class="contrast-calculator" data-palette={JSON.stringify(brandSettings.palette)}>
                    <div class="calculator-controls">
                        <div class="control-group">
                            <label for="bg-color">Background Color</label>
                            <select id="bg-color">
                                <!-- Populated by JS -->
                            </select>
                        </div>
                        <button id="swap-colors" class="swap-btn" aria-label="Swap background and text colors" title="Swap Colors">
                            ⇄
                        </button>
                        <div class="control-group">
                            <label for="text-color">Text Color</label>
                            <select id="text-color">
                                <!-- Populated by JS -->
                            </select>
                        </div>
                    </div>

                    <div id="preview-area" class="preview-area">
                        <h4>Preview Text</h4>
                        <p>The quick brown fox jumps over the lazy dog.</p>
                    </div>

                    <div class="results">
                        <p class="results-ratio"><strong>Contrast Ratio:</strong> <span id="ratio-display"></span></p>
                        
                        <table>
                            <thead>
                                <tr>
                                    <th>Standard</th>
                                    <th>Normal Text</th>
                                    <th>Large Text (18pt+ / 14pt bold)</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td><strong>WCAG AA</strong></td>
                                    <td id="aa-normal"></td>
                                    <td id="aa-large"></td>
                                </tr>
                                <tr>
                                    <td><strong>WCAG AAA</strong></td>
                                    <td id="aaa-normal"></td>
                                    <td id="aaa-large"></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>

                <h3>Negative Examples</h3>
                <p>The following examples illustrate color combinations that fail WCAG AA compliance due to insufficient contrast.</p>
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem; margin-bottom: 2rem;">
                    <div style="background-color: #ffffff; color: #cccccc; padding: 1.5rem; border: 1px solid #e0e0e0;">
                        <h4 style="margin-top: 0; color: inherit;">Do Not Use</h4>
                        <p style="margin-bottom: 0;">Light gray text on white background (Fails WCAG AA)</p>
                    </div>
                    <div style="background-color: #333333; color: #000000; padding: 1.5rem; border: 1px solid #e0e0e0;">
                        <h4 style="margin-top: 0; color: inherit;">Do Not Use</h4>
                        <p style="margin-bottom: 0;">Black text on dark gray background (Poor visibility)</p>
                    </div>
                </div>

                <h3>Non-Text Contrast (UI Elements)</h3>
                <p>Interactive components must meet contrast requirements as well; Splitli follows the WCAG 2.1 AA non-text contrast standard of 3:1 for buttons, icons, focus indicators, and active navigation states. Sufficient contrast ensures users with low vision or cognitive disabilities can easily identify where actions can occur.</p>

                <h3>Inline Links</h3>
                <p>Inline links appear within body text and must be clear and identifiable so users can easily distinguish them from the surrounding content.</p>
                <ul>
                    <li><strong>Text Decoration:</strong> Inline links are always underlined. Underlines ensure links remain recognizable for all users. Links should never rely on color alone to indicate interactivity, as some users are color deficient, low vision, or utilize assistive technologies. </li>
                    <li><strong>Color Contrast:</strong> Link text must meet a minimum contrast ratio of 4.5:1 against the background to maintain readability. Additionally, the link color should remain visually distinct from the surrounding body text.</li>
                    <li><strong>Hover & Focus:</strong> When users hover over or focus on a link, a visible state change should occur to confirm interactivity. Some examples of acceptable interaction indicators include a shade change to the link color, a background highlight, an underline, and a visible focus outline for keyboard navigation.</li>
                </ul>
            </section>